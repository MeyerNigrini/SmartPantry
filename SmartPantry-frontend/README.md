## 🧹 Prettier Commands

```bash
# Format all files in the project
npm run format

# Check for formatting issues without writing changes (useful for CI)
npm run format:check

# Format a specific file
npx prettier --write src/components/YourComponent.tsx
```
